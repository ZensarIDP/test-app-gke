apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-app-gke
  namespace: default
  labels:
    app: test-app-gke
spec:
  replicas: 2
  selector:
    matchLabels:
      app: test-app-gke
  template:
    metadata:
      labels:
        app: test-app-gke
    spec:
      containers:
      - name: test-app-gke
        image: asia-east1-docker.pkg.dev/dev-zephyr-352206/test-app-gke/test-app-gke:latest
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3000"
        - name: OPENWEATHER_API_KEY
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: openweather-api-key
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
---
apiVersion: v1
kind: Service
metadata:
  name: test-app-gke-service
  namespace: default
spec:
  selector:
    app: test-app-gke
  ports:
  - port: 80
    targetPort: 3000
  type: LoadBalancer
